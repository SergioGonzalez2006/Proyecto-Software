info:
  name: Get Usuario
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api-kazukiTurismo/api/usuarios
  body:
    type: json
    data: |-
      [
        {
          "idUsuario": 0,
          "nombre": "string",
          "correo": "string",
          "contrasena": "string",
          "rolUsuario": "string"
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
