info:
  name: Post Usuario
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api-kazukiTurismo/api/usuarios
  body:
    type: json
    data: |-
      {
        "idUsuario": 0,
        "nombre": "Andres",
        "correo": "ander@gmail.com",
        "contrasena": "melo123",
        "rolUsuario": "CLIENTE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
